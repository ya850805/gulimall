server:
  port: 9000
spring:
  datasource:
    url: jdbc:mysql://localhost:3305/gulimall_oms
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto